import { test } from 'vitest'

test.todo('splitIntoSignGroups', () => {
  // const signs = signsByDescriptiveName(['Gehweg', 'Radfahrer frei'])
  // expect(splitIntoSignGroups(signs)).toMatchObject(['footway', 'path'])
})
