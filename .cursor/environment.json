{
  "install": "curl -fsSL https://bun.sh/install | bash -s \"bun-v1.3.13\" && export BUN_INSTALL=\"$HOME/.bun\" && export PATH=\"$BUN_INSTALL/bin:$PATH\" && bun --version && bun install --frozen-lockfile"
}
